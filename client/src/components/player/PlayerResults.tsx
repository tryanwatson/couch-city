export default function PlayerResults() {
  return (
    <div className="player-results-screen">
      <h2 className="results-title">Results</h2>
      <p className="results-subtitle">Check the host screen for the leaderboard!</p>
    </div>
  );
}
